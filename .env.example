# ==============================================================================
# Food Stock Control System - Environment Variables Template
# ==============================================================================
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual credentials
# 3. NEVER commit .env to version control (already in .gitignore)
# ==============================================================================

# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================

# Prisma database connection string (PostgreSQL via Supabase Session Pooler)
# IMPORTANT: Must use Session mode pooler (pooler.supabase.com), NOT direct connection (db.supabase.co)
# Format: postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-[REGION].pooler.supabase.com:5432/postgres
# Used by: Prisma ORM for all database operations
# Source: Supabase Dashboard → Settings → Database → Connection string (Session mode)
# Example: postgresql://postgres.abcd1234:mypassword@aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres
DATABASE_URL="postgresql://postgres.[YOUR-PROJECT-REF]:[YOUR-PASSWORD]@aws-[REGION].pooler.supabase.com:5432/postgres"

# ==============================================================================
# SUPABASE CONFIGURATION
# ==============================================================================

# Supabase project URL
# Used by: @supabase/supabase-js client for REST API and Realtime features
# Source: Supabase Dashboard → Settings → API → Project URL
# Format: https://[YOUR-PROJECT-REF].supabase.co
# Example: https://abcd1234efgh5678.supabase.co
SUPABASE_URL="https://[YOUR-PROJECT-REF].supabase.co"

# Supabase anonymous (public) API key
# Used by: Client-side Supabase operations with Row Level Security (RLS)
# Security: Safe to expose in browser - works with RLS policies
# Source: Supabase Dashboard → Settings → API → Project API keys → anon public
# Note: This is a JWT token - looks like eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_ANON_KEY="your-supabase-anon-key-here"

# Supabase service role (admin) API key
# Used by: Server-side operations that bypass RLS (admin operations)
# Security: NEVER expose to client - server-only, has full database access
# Source: Supabase Dashboard → Settings → API → Project API keys → service_role secret
# Note: This is a JWT token - looks like eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_KEY="your-supabase-service-role-key-here"

# ==============================================================================
# AUTHENTICATION CONFIGURATION
# ==============================================================================

# Secret key for JWT token signing and session encryption
# Used by: nuxt-auth-utils for secure session management
# Security: NEVER expose to client - server-only
# Generate new secret using one of these methods:
#   - Node.js: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#   - OpenSSL: openssl rand -base64 32
#   - Online: https://generate-secret.vercel.app/32
# Example: JsXaJojj4Uj7XyZcegRjtXGDsKPOnrtksH7IJG9WBNo=
AUTH_SECRET="generate-a-secure-random-string-here"

# ==============================================================================
# APPLICATION CONFIGURATION
# ==============================================================================

# Public base URL of the application
# Used by: Server-side redirects, email links, PWA manifest
# Development: http://localhost:3000
# Production: https://your-domain.vercel.app (replace with your actual Vercel URL)
# Note: No trailing slash
NUXT_PUBLIC_SITE_URL="http://localhost:3000"

# Default currency for the application
# Used by: Price formatting, financial calculations throughout the app
# Format: ISO 4217 currency code (SAR = Saudi Riyal)
# Common values: SAR, USD, EUR, GBP
NUXT_PUBLIC_CURRENCY="SAR"

# ==============================================================================
# IMPORTANT NOTES
# ==============================================================================
# - All variables prefixed with NUXT_PUBLIC_ are exposed to the client browser
# - Variables without NUXT_PUBLIC_ are SERVER-ONLY and never sent to the client
# - For production: Set these in Vercel Dashboard → Settings → Environment Variables
# - Never commit the .env file to Git (already in .gitignore)
# ==============================================================================
