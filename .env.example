# ============================================
# Food Stock Control System - Environment Variables
# ============================================
# Copy this file to .env and fill in your actual values
# Never commit .env to version control!

# ============================================
# DATABASE CONFIGURATION
# ============================================
# PostgreSQL connection string from Supabase
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Get this from: Supabase Project Settings > Database > Connection String > URI
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"

# ============================================
# SUPABASE CONFIGURATION
# ============================================
# Get these from: Supabase Project Settings > API

# Supabase Project URL (PUBLIC - sent to client)
# Format: https://[YOUR-PROJECT-REF].supabase.co
SUPABASE_URL="https://[YOUR-PROJECT-REF].supabase.co"

# Supabase Anonymous Key (PUBLIC - sent to client)
# This key is safe to use in client-side code
SUPABASE_ANON_KEY="your-supabase-anon-key-here"

# Supabase Service Role Key (SERVER-ONLY - never expose to client!)
# This key bypasses Row Level Security - keep it secret!
SUPABASE_SERVICE_KEY="your-supabase-service-role-key-here"

# ============================================
# AUTHENTICATION CONFIGURATION
# ============================================
# Secret key for JWT token signing (SERVER-ONLY)
# Generate a secure random string using:
#   - Node.js: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#   - OpenSSL: openssl rand -base64 32
#   - Or use: https://generate-secret.vercel.app/32
AUTH_SECRET="generate-a-secure-random-string-here"

# ============================================
# APPLICATION CONFIGURATION
# ============================================
# Production site URL (PUBLIC - sent to client)
# Use http://localhost:3000 for local development
# Use your Vercel deployment URL for production
NUXT_PUBLIC_SITE_URL="http://localhost:3000"

# Currency code (PUBLIC - sent to client)
# Default: SAR (Saudi Riyal)
NUXT_PUBLIC_CURRENCY="SAR"

# ============================================
# NOTES
# ============================================
# - All variables prefixed with NUXT_PUBLIC_ are exposed to the client
# - Variables without NUXT_PUBLIC_ are server-only
# - Update these values before running the application
# - For production deployment, set these in Vercel dashboard
