No user should be able to edit or delete the main Admin account.
No user should be able to edit or delete any account on his level.

The "Resend Email" button does not currently exist in the PO detail page UI (app/pages/orders/pos/[id].vue) — the server-side API is implemented but the frontend button hasn't been added yet. The documentation references it as intended; you'll need to add the button to the page when you're ready.
